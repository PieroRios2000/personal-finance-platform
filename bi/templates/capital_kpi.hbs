{{#each data}}
<div class="kpis">
  <div class="kpi cap"><div class="label">Total capital, latest month</div>
    <div class="value">{{currency}} {{total}}</div>
    <div class="note">{{month}} &middot; savings {{savings}} + investments {{investments}}</div></div>
</div>
{{/each}}
