{{#each data}}
<div class="kpis">
  <div class="kpi debt"><div class="label">Debt at the end of the period</div>
    <div class="value">{{currency}} {{debt}}</div>
    <div class="note">{{calendar_month}} &middot; credit card balance owed, in the last month of your selection</div></div>
</div>
{{/each}}
