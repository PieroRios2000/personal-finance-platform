{{#each data}}
<div class="kpis">
  <div class="kpi range"><div class="label">Period analysed</div>
    <div class="value small">{{from}} &rarr; {{to}}</div>
    <div class="note">{{months}} closed month(s), from the movements you have filtered</div></div>
</div>
{{/each}}
